<?php

/**
 * Implements hook_theme().
 */
function nc_liste_theme($existing, $type, $theme, $path) {
    return array(
        'consultationsp' => array(
            'template' => 'consultationsp',
            'variables' => array(
                'data' => NULL,
            )
        ),
    );
}