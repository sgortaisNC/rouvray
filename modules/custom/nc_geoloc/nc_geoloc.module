<?php

/**
 * Implements hook_theme().
 */
function nc_geoloc_theme($existing, $type, $theme, $path) {
    return [
        'geoloc' => [
            'template' => 'geoloc',
            'variables' => [
                'data' => NULL,
            ],
        ],
    ];
}