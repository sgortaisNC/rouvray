<?php

/**
 * Implements hook_theme().
 */
function nc_translator_theme($existing, $type, $theme, $path) {
    return [
        'translator' => [
            'template' => 'translator',
            'variables' => [
                'data' => NULL,
            ],
        ],
    ];
}