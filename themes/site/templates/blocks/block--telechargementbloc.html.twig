{% if(isEmpty != true) %}
    <div class="bloc liste_docs">
        {% if label %}
            <h3 class="c-left">{{ label }}</h3>
        {% endif %}

        {% block content %}
            {{ content }}
        {% endblock %}
    </div>
{% endif %}


